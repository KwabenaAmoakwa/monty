#include "monty.h"
/**
 * nop - print the stack as string
 * @head: head of the stack
 * @line_number: line number
 * Return: no return
 */
void nop(stack_t **head, unsigned int line_number)
{
	(void)head;
	(void)line_number;
}
